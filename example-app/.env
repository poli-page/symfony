APP_ENV=dev
APP_DEBUG=1
APP_SECRET=example-app-not-secret

# POLI_PAGE_API_KEY is sourced from the bundle repo's root .env via
# example-app/bootstrap.php. Override here (or in .env.local, or via a
# shell export) if you want a different key for this app.
